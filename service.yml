name: react
type: service
from: nginx
fromVersionConstraint: "^2.0.0"
fromVersion: "2.0.1"
title: Nginx (React)
labels:
- nginx
- react

build:
  connect: true
  boilerplates:
  - name: react
    title: React boilerplate
    repo: https://github.com/wodby/react-boilerplate
    branch: main
